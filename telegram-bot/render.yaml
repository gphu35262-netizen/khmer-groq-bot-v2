services:
  - type: worker
    name: khmer-ai-bot
    env: node
    buildCommand: pnpm install
    startCommand: pnpm start
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: GROQ_API_KEY
        sync: false
